# TouchSensor

`class TouchSensor(port)`

Object that allows interaction with a connected touch sensor.

Parameters:

 * port ([Input](/input/)): The port on the EV3 Brick, where the sensor is connected.

## Methods

`pressed()`

Returns True if the sensor button is pressed and False otherwise.